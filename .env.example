# Rename this file to .env and adjust the values before running locally.
# The application falls back to the defaults below if the variables are missing,
# but production deployments should override them.

PORT=3000
JWT_SECRET=change-me
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password
LEGACY_ADMIN_USERNAME=admin
LEGACY_ADMIN_PASSWORD=password
TIMEKEEPER_EMAIL=timekeeper@example.com
TIMEKEEPER_PASSWORD=password
TIMEKEEPER_DEPOT=Makumbura
LEGACY_TIMEKEEPER_USERNAME=timekeeper
